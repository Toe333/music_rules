"""Package data — canonical rule corpus and JSON Schema.

Files in this package are addressable via ``importlib.resources``::

    from importlib.resources import files
    json_path = files("music_rules.data") / "rules_combined.json"
"""
